package com.arya.collection.compare;

public class DogFeatureComparble implements Comparable<Dog> {

	@Override
	public int compareTo(Dog feature1, Dog feature2) {
		return dog1.getfeature().compareTo.(dog2.getfeature);
	}
}
